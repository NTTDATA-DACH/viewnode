## Codebase Patterns
- Classify Kubernetes API retrieval failures in `srv/api_errors.go` with typed wrappers that implement `Unwrap()`, so `cmd/root.go` can branch with `errors.As` while preserving the original low-level error for `errors.Is` and fatal output.
- For exact fatal/warn log assertions in `cmd/root_test.go`, pin Logrus to a `TextFormatter` with quoting and timestamps disabled; otherwise embedded URLs and quotes make the captured output unstable.
- Compatibility coverage for new retrieval-error handling belongs in both layers: keep classifier-scope assertions in `srv/api_errors_test.go`, and add root-level message assertions in `cmd/root_test.go` for any path that must not change user-facing wording.

# Ralph Progress Log
Started: Tue Mar 24 17:06:47 CET 2026
---
## [2026-03-24 17:08:25 CET] - #27-001
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-001.txt
- Added `srv.ScopedEOFError` as a dedicated decorated error for scoped Kubernetes API `Get ...: EOF` failures and preserved the wrapped original error with `Unwrap()`.
- Extended the existing decorated error types to support unwrapping consistently and added focused regression coverage for scoped EOF classification and non-EOF compatibility.
- Files changed: `srv/api_errors.go`, `srv/api_errors_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/.codex-last-message-iter-001.txt`
- **Learnings for future iterations:**
  - Patterns discovered: retrieval-error decoration in `srv/api_errors.go` is the intended boundary for special-case Kubernetes API failures before the root command translates them into user-facing fatal messages.
  - Gotchas encountered: preserving wrapped errors requires `Unwrap()` on the typed decorators; otherwise `errors.Is` cannot retain access to the original low-level failure.
  - Useful context: the targeted EOF slice is currently matched from the surfaced error string shape `Get ...: EOF`, which keeps the classifier narrow without pushing string matching into `cmd/root.go`.
---
## [2026-03-24 17:13:51 CET] - #27-002
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-002.txt
- Routed root load/filter failure handling through `handleLoadAndFilterError` and added a dedicated `srv.ScopedEOFError` branch that emits a proxy-configuration hint while keeping the original EOF detail in the fatal output.
- Added focused regression coverage in `cmd/root_test.go` to assert the scoped EOF fatal message includes both the new guidance and the wrapped low-level error text.
- Files changed: `cmd/root.go`, `cmd/root_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/.codex-last-message-iter-002.txt`
- **Learnings for future iterations:**
  - Patterns discovered: root-level user-facing translation for retrieval failures now flows through `handleLoadAndFilterError`, keeping classification in `srv` and message wording in `cmd/root.go`.
  - Gotchas encountered: when asserting fatal log text in tests, normalize the Logrus formatter or the default quoted output will escape embedded quotes from raw Kubernetes request URLs.
  - Useful context: the proxy hint is intentionally scoped to decorated `srv.ScopedEOFError` values, so generic EOFs and other fatal paths still fall through to their existing wording.
---
## [2026-03-24 17:14:17 CET] - #27-003
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-003.txt
- Added focused compatibility regression tests to lock in the existing unauthorized fatal path, forbidden warning fallback, and generic non-EOF fatal wording while asserting none of them pick up the proxy hint.
- Added a classifier regression test proving out-of-scope `Post ...: EOF` errors are left undecorated, so the proxy guidance remains limited to the intended scoped `Get ...: EOF` slice.
- Files changed: `cmd/root_test.go`, `srv/api_errors_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/.codex-last-message-iter-003.txt`
- **Learnings for future iterations:**
  - Patterns discovered: compatibility around `handleLoadAndFilterError` is best pinned with message-level tests in `cmd/root_test.go`, because the root command owns the user-facing translation of decorated service errors.
  - Gotchas encountered: `srv.DecorateError` only scopes the proxy-hint path to surfaced `Get ...: EOF` strings, so out-of-scope EOF regressions should be tested at the decoration boundary rather than by widening root-command branching.
  - Useful context: the forbidden node path is intentionally non-fatal and should keep returning `true` from `handleLoadAndFilterError` so the caller can continue with pod-derived node names.
---
## [2026-03-24 17:16:24 CET] - #27-004
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-004.txt
- Added a repeated-call regression test for `handleLoadAndFilterError` that asserts the scoped EOF proxy-hint fatal output is emitted identically on each occurrence.
- Validated the stateless behavior with `go test ./cmd` and repository-level `make test`.
- Files changed: `cmd/root_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/.codex-last-message-iter-004.txt`
- **Learnings for future iterations:**
  - Patterns discovered: repeated fatal-path behavior in `cmd/root.go` can be locked down by invoking the handler multiple times in one test while reusing the same decorated error value.
  - Gotchas encountered: for deterministic Logrus assertions, reset the output buffer inside the loop but keep the formatter fixed; that isolates repeated-call output without introducing logger state drift.
  - Useful context: the scoped EOF path is already stateless, so this story is covered by regression proof rather than production-code changes.
---
## [2026-03-24 17:22:00 CET] - #27-005
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-005.txt
- Added the missing root-command regression that proves an out-of-scope `Post ...: EOF` failure keeps the existing generic fatal wording and does not pick up the proxy-configuration hint.
- Re-ran focused regression suites for `./cmd` and `./srv`, then validated the repository requirement with `make test`.
- Files changed: `cmd/root_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/.codex-last-message-iter-005.txt`
- **Learnings for future iterations:**
  - Patterns discovered: compatibility for narrow error classifiers should be proven at the root-message layer with a concrete out-of-scope example, not only at the classifier boundary.
  - Gotchas encountered: a raw `errors.New("Post ...: EOF")` value is enough for the out-of-scope root-path regression because the proxy hint is only gated by decorated `srv.ScopedEOFError` values.
  - Useful context: story `#27-005` did not require production-code changes because the existing handler behavior already matched the intended scope; the missing artifact was regression proof.
---
