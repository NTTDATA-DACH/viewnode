# Ralph Progress Log
## Codebase Patterns
- Top-level Cobra commands without subcommands render under `Additional help topics` in root help output, so help assertions should match that section instead of `Available Commands`.
- Commands that honor the global `--kubeconfig` flag must pass the root Cobra command into `config.Initialize`; using the leaf subcommand directly can miss the root-level flag lookup.
- Commands that persist kubeconfig changes must set `ClientConfigLoadingRules.ExplicitPath` from `Setup.KubeCfgPath` before `clientcmd.ModifyConfig`, or `--kubeconfig` can write changes to the wrong file.
- Cobra tests that need inherited persistent flags should execute through a root command tree with `Execute()`; calling a leaf command's `RunE` directly skips the normal flag propagation.
- `README.md` command listings and CLI usage examples are maintained manually, so user-facing CLI command changes should update the README in the same change.
- Cobra command groups in `cmd/<name>` follow the `ctx` pattern: define the group command in `<name>.go`, keep each subcommand in its own file, and register them together from the package `init()`.
- Namespace current-state lookups should read the active context from `ClientConfig.RawConfig()` and print `default` when the context namespace is unset, so the CLI mirrors Kubernetes kubeconfig behavior.
- Namespace list rendering should derive its active namespace marker from `ClientConfig.RawConfig()` with a `default` fallback, because `Setup.Namespace` does not preserve the distinction between implicit and explicit kubeconfig namespaces.
- Shared alphabetical list rendering logic for command output belongs in `cmd/internal/listing`, with package-specific entry types built from a common active-state preparer instead of duplicating sort-and-mark loops in command packages.

Started: Thu Mar 12 07:29:15 CET 2026
---

## [2026-03-14 14:08:54 CET] - #50-001
Codex output: scripts/ralph/.codex-last-message-iter-*.txt (no matching file present in workspace during this iteration)
- Added `ns get-current` and `ns set-current` command definitions in dedicated files and registered them under the existing `ns` command group.
- Added focused command-structure coverage to verify both subcommands are discoverable from `ns` and expose the required legacy aliases.
- Files changed: `AGENTS.md`, `cmd/ns/getcurrent.go`, `cmd/ns/ns.go`, `cmd/ns/ns_test.go`, `cmd/ns/setcurrent.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: namespace command groups should mirror `cmd/ctx` by keeping one file per subcommand and centralizing registration in `ns.go`.
  - Gotchas encountered: this story is only command wiring, so behavior should remain unimplemented until later iterations to avoid skipping PRD ordering.
- Useful context: `NsCmd.Find([]string{"subcommand"})` is a simple way to assert subcommand registration and alias metadata without building a full root tree.
---

## [2026-03-14 14:11:50 CET] - #50-002
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-1.txt
- Implemented `viewnode ns get-current` to initialize config through the root command, read the active kubeconfig context namespace from raw config, and print `default` when no namespace is set.
- Added tests for inherited `--kubeconfig` propagation via `Execute()`, `default` namespace fallback from kubeconfig, raw-config read failures, and initialization failures.
- Files changed: `cmd/ns/getcurrent.go`, `cmd/ns/ns_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: namespace current-state reads should inspect `ClientConfig.RawConfig().Contexts[CurrentContext].Namespace` instead of relying only on `Setup.Namespace`, because the CLI needs to distinguish explicit namespace state from Kubernetes' implicit `default`.
  - Gotchas encountered: `config.Initialize` still constructs clientsets during command setup, so tests for kubeconfig-reading behavior are easier to keep deterministic by overriding narrow package-level seams.
- Useful context: a small synthetic root command with `Execute()` is sufficient to verify inherited `--kubeconfig` handling without wiring the entire application root.
---

## [2026-03-14 14:15:46 CET] - #50-003
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-2.txt
- Implemented `viewnode ns set-current <namespace>` to initialize config from the root command, validate the namespace against the current cluster, update only the active kubeconfig context namespace, and print a success message after persistence.
- Added focused tests for inherited `--kubeconfig` propagation, successful active-context mutation, missing namespace rejection without writes, validation and raw-config error paths, persistence failures, and explicit kubeconfig-path writes.
- Files changed: `AGENTS.md`, `cmd/ns/setcurrent.go`, `cmd/ns/setcurrent_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: kubeconfig writes should go through `clientcmd.ModifyConfig` with loading rules that copy `setup.KubeCfgPath` into `ExplicitPath`, so root `--kubeconfig` changes persist to the intended file.
  - Gotchas encountered: namespace validation should happen before `RawConfig()` mutation and persistence so error cases cannot partially update the kubeconfig.
  - Useful context: mutating `rawConfig.Contexts[rawConfig.CurrentContext].Namespace` preserves other contexts unchanged, which is the right scope for namespace current-state updates.
---

## [2026-03-14 14:18:00 CET] - #50-004
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-3.txt
- Expanded README namespace command documentation to cover `ns get-current`, `ns set-current`, and the legacy `getcurrent` / `setcurrent` aliases alongside the existing `ns ls` alias.
- Added an extra `ns set-current` test for the missing-active-context kubeconfig error path and verified the full suite with `make test`.
- Files changed: `README.md`, `cmd/ns/setcurrent_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: manual CLI examples in `README.md` should show both the canonical subcommand form and any supported legacy aliases when a command family preserves backward-compatible names.
  - Gotchas encountered: `ns set-current` has an additional kubeconfig guard when `RawConfig().CurrentContext` is unset or missing from the contexts map, so tests should cover that explicit error path.
  - Useful context: the namespace README section now documents the full current-state workflow (`list`, `get-current`, `set-current`) in one example block, matching how the `ctx` section is presented.
---

## [2026-03-12 10:18:38 CET] - #47-001
Codex output: scripts/ralph/.codex-last-message-iter-*.txt (no matching file present in workspace during this iteration)
- Added a new top-level `ns` Cobra command in a dedicated `cmd/ns` package with help text for namespace-related operations.
- Registered `ns` on the root command so it is visible from `viewnode --help` without changing existing command behavior.
- Added test coverage for root help output and the new command metadata.
- Files changed: `cmd/root.go`, `cmd/root_test.go`, `cmd/ns/ns.go`, `cmd/ns/ns_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: top-level command groups should live in their own `cmd/<name>` package and be registered from `cmd/root.go` alongside `ctx`.
  - Gotchas encountered: Cobra places command groups with no subcommands under `Additional help topics`, which affects help-output tests.
  - Useful context: the branch from `prd.json` already existed locally and on `origin`, so no branch creation was needed for issue `#47`.
---

## [2026-03-12 10:23:10 CET] - #47-002
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-1.txt
- Implemented `viewnode ns list` to initialize shared Kubernetes config, load namespaces from the cluster API, sort names deterministically, and print the current namespace with `[*]`.
- Registered the `list` subcommand under the existing `ns` command group and kept command text aligned with the existing Cobra structure.
- Updated the root help assertion to match Cobra's rendered command table after `ns` gained a subcommand.
- Files changed: `AGENTS.md`, `cmd/ns/list.go`, `cmd/ns/ns.go`, `cmd/root_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: namespace enumeration belongs to the Kubernetes API clientset, while the current namespace still comes from `config.Setup.Namespace` after shared initialization.
  - Gotchas encountered: once a top-level Cobra topic gains a subcommand, it moves from `Additional help topics` into `Available Commands`, which changes root help assertions.
  - Useful context: `config.Initialize` reads `kubeconfig` from the command passed into it, so subcommands should resolve and pass `c.Root()` to honor the global flag.
---

## [2026-03-12 10:24:55 CET] - #47-003
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-2.txt
- Added focused automated coverage for `viewnode ns list` using a narrow seam around config setup and namespace discovery.
- Verified sorted namespace output, current namespace marker rendering, inherited root `--kubeconfig` handling, and an API discovery error path.
- Files changed: `AGENTS.md`, `cmd/ns/list.go`, `cmd/ns/list_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: `cmd/ns/list.go` can be tested without a live cluster by overriding package-level seams for config initialization, config access, and namespace discovery.
  - Gotchas encountered: inherited Cobra persistent flags are only available after command execution wiring, so tests should call `Execute()` on a root command tree when validating root flag behavior.
  - Useful context: the error contract for namespace discovery is `getting kubernetes namespaces failed (<cause>)`, so future changes should preserve that wrapper for callers and tests.
---

## [2026-03-12 10:26:45 CET] - #47-004
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-3.txt
- Updated the manual CLI reference in `README.md` to include the top-level `ns` command in the root command table.
- Added a `Namespace command` usage example documenting `viewnode ns list` output and the `ns ls` alias.
- Files changed: `AGENTS.md`, `README.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: `README.md` is the manual source for user-facing command listings and command examples, so CLI command additions need a README update in the same change.
  - Gotchas encountered: there is no generated command reference step in this repository, so documentation drift is easy if README updates are skipped.
  - Useful context: the existing command documentation groups related subcommands into short example sections such as `Context command`, which is the right pattern to reuse for future command groups.
---

## [2026-03-15 14:34 CET] - #48-001
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-4.txt
- Added `cmd/internal/listing` with explicit `ContextEntry` and `NamespaceEntry` types plus a shared helper that sorts raw names and marks the active item.
- Added focused tests covering deterministic ordering and active-state behavior for the shared helper without changing `ctx list` or `ns list` command behavior yet.
- Files changed: `cmd/internal/listing/entries.go`, `cmd/internal/listing/entries_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: list-row preparation can be shared cleanly without moving kubeconfig or namespace discovery logic out of the command packages.
  - Gotchas encountered: this repo already treats command-structure refactors as separate story slices, so keep helper introduction separate from command rewiring.
  - Useful context: `make test` includes the new internal package automatically, so story-level helper coverage is visible in the standard validation run.
---

## [2026-03-15 14:28:08 CET] - #48-002
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-1.txt
- Refactored `viewnode ctx list` to render through `cmd/internal/listing.PrepareContextEntries` while keeping raw kubeconfig lookup and the existing `[marker] name` output contract in `cmd/ctx/list.go`.
- Added focused command tests for successful context listing through a root command tree and for initialization failure propagation without partial stdout output.
- Files changed: `cmd/ctx/ctx_test.go`, `cmd/ctx/list.go`, `cmd/ctx/list_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: command packages can adopt the `initializeConfig` / `currentSetup` seam pattern for deterministic tests without moving command-specific kubeconfig reads out of the command implementation.
  - Gotchas encountered: `config.Initialize` caches global setup state, so direct failure-path tests are more reliable when they override narrow package-level seams instead of depending on repeated real initialization.
  - Useful context: `clientcmd.NewDefaultClientConfig` built from the shared kubeconfig fixture is enough to exercise `RawConfig()`-based context listing logic without creating a live clientset.
---

## [2026-03-15 14:30:56 CET] - #48-003
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-2.txt
- Refactored `viewnode ns list` to render through `cmd/internal/listing.PrepareNamespaceEntries` while keeping namespace discovery on the existing Kubernetes API path.
- Switched active namespace detection to `ClientConfig.RawConfig()` with `default` fallback semantics so unset context namespaces still render as active correctly.
- Added focused tests for successful namespace listing through the shared entry path, `default` fallback behavior, namespace discovery failure without partial stdout, and initialization failure without partial stdout.
- Files changed: `cmd/ns/list.go`, `cmd/ns/list_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: list-style namespace commands should derive the active namespace from raw kubeconfig context data instead of `Setup.Namespace` when Kubernetes `default` fallback semantics matter.
  - Gotchas encountered: reusing namespace fixtures across tests can silently carry the wrong active namespace unless the raw config is adjusted explicitly in the test setup.
  - Useful context: `PrepareNamespaceEntries` now owns the ordering for `ns list`, so command tests can focus on data sourcing and output contracts rather than local sort behavior.
---

## [2026-03-15 14:33:15 CET] - #48-004
Codex output: /Users/adam.boczek/Development/Workspace/NTTDATA/viewnode/scripts/ralph/.codex-last-message-iter-5.txt
- Added the missing command-level `ctx list` test for raw kubeconfig failure propagation and verified that it produces no partial stdout output.
- Ran `make test` to validate the full list-command refactor across the repository.
- Files changed: `cmd/ctx/list_test.go`, `scripts/ralph/.codex-last-message-iter-5.txt`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: `ctx list` failure-path coverage is easiest to keep deterministic by injecting a stub `clientcmd.ClientConfig` that fails only on `RawConfig()`.
  - Gotchas encountered: existing `.codex-last-message-iter-*` files may not sort numerically by modification time, so Ralph progress entries should point to the file created in the current iteration.
  - Useful context: `make test` passed after the additional `ctx list` failure-path coverage, so the shared list-entry refactor now has command-level success and failure validation for both `ctx list` and `ns list`.
---
